/**
 * An object mapping US state abbreviations to their full names.
 * @example
 * import { states } from '@veroproducts/data';
 * console.log(states.CA); // "California"
 */
export const states: { [key: string]: string };
